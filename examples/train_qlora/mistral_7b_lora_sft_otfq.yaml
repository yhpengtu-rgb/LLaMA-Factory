### model
model_name_or_path: unsloth/mistral-7b-instruct-v0.3
quantization_bit: 4  # choices: [8 (bnb/hqq/eetq), 4 (bnb/hqq), 3 (hqq), 2 (hqq)]
quantization_method: bnb  # choices: [bnb, hqq, eetq]
trust_remote_code: true

### method
stage: sft
do_train: true
finetuning_type: lora
lora_rank: 8
lora_target: q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj
# use_unsloth: true

### dataset
dataset: alpaca_100proteins
template: default
cutoff_len: 9000
max_samples: 10000000000
overwrite_cache: true
preprocessing_num_workers: 16
dataloader_num_workers: 4
tokenized_path: saves/mistral-7b/dataset/sft

### output
output_dir: saves/mistral-7b/lora/sft
logging_steps: 10
save_steps: 500
plot_loss: true
overwrite_output_dir: true
save_only_model: false
report_to: none  # choices: [none, wandb, tensorboard, swanlab, mlflow]

### train
per_device_train_batch_size: 8
gradient_accumulation_steps: 1
learning_rate: 1.0e-4
num_train_epochs: 10.0
lr_scheduler_type: cosine
warmup_ratio: 0.1
bf16: true
ddp_timeout: 180000000

### eval
# val_size: 0.1
# per_device_eval_batch_size: 1
# eval_strategy: steps
# eval_steps: 500
